diff --git a/web/auth.go b/web/auth.go
index bf44ef0..2fa476c 100644
--- a/web/auth.go
+++ b/web/auth.go
@@ -2,6 +2,7 @@ package web
 
 import (
 	"net/http"
+	"os"
 	"time"
 
 	"github.com/jeessy2/ddns-go/v6/config"
@@ -14,6 +15,12 @@ type ViewFunc func(http.ResponseWriter, *http.Request)
 // Auth 验证Token是否已经通过
 func Auth(f ViewFunc) ViewFunc {
 	return func(w http.ResponseWriter, r *http.Request) {
+		// Home Assistant Ingress authenticates the request before forwarding it.
+		if os.Getenv("DDNS_GO_HA_INGRESS") == "1" {
+			f(w, r)
+			return
+		}
+
 		cookieInWeb, err := r.Cookie(cookieName)
 		if err != nil {
 			http.Redirect(w, r, "./login", http.StatusTemporaryRedirect)
diff --git a/web/save.go b/web/save.go
index 2fa45bb..f9287ad 100755
--- a/web/save.go
+++ b/web/save.go
@@ -3,6 +3,7 @@ package web
 import (
 	"encoding/json"
 	"net/http"
+	"os"
 	"strings"
 
 	"github.com/jeessy2/ddns-go/v6/config"
@@ -67,6 +68,21 @@ func checkAndSave(request *http.Request) string {
 		conf.Password = hashedPwd
 	}
 
+	// Home Assistant Ingress is the user-facing authentication boundary. Keep
+	// ddns-go's own credentials populated for its configuration validation.
+	if os.Getenv("DDNS_GO_HA_INGRESS") == "1" {
+		if conf.Username == "" {
+			conf.Username = "homeassistant"
+		}
+		if conf.Password == "" {
+			hashedPwd, err := conf.CheckPassword(os.Getenv("DDNS_GO_HA_INGRESS_PASSWORD"))
+			if err != nil {
+				return err.Error()
+			}
+			conf.Password = hashedPwd
+		}
+	}
+
 	// 帐号密码不能为空
 	if conf.Username == "" || conf.Password == "" {
 		return util.LogStr("必须输入用户名/密码")
